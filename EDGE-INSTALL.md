# Microsoft Edge 安装指南

本扩展使用 Chromium Manifest V3 标准，Chrome 与 Microsoft Edge 共用同一套代码。

## 安装前说明

部分新版 Edge 稳定版可能不显示“开发人员模式”和“加载解压缩的扩展”。如果 `edge://extensions/` 中没有这两个入口，并不是插件压缩包损坏，也不是用户漏看了设置。

- **开发者自己测试**：使用 Microsoft Edge Dev 或 Canary，然后按下方步骤加载；
- **提供给普通用户**：应将扩展发布到 Microsoft Edge 加载项商店，用户从商店正常安装；
- **已经安装 Chrome 版**：无需修改代码，本项目的 Manifest V3 扩展与 Edge 兼容。

## 安装步骤

1. 将下载的 ZIP 压缩包解压到一个固定文件夹。安装后不要删除或移动该文件夹；
2. 在 Microsoft Edge 地址栏输入 `edge://extensions/` 并回车；
3. 打开左侧的“开发人员模式”；
4. 点击“加载解压缩的扩展”；
5. 选择解压后的文件夹。应当选择其中直接包含 `manifest.json` 的那一级；
6. 确认扩展列表中出现“B站推荐上一批”；
7. 打开或刷新 `https://www.bilibili.com/`。

正常点击一次“换一换”后，下方的“上一批”按钮会变为可用状态。

## 更新方法

下载并解压新版本，用新文件替换旧文件，然后在 `edge://extensions/` 中找到本扩展并点击“重新加载”。

## 常见问题

- **提示清单文件缺失**：选择错了文件夹层级，请选择直接包含 `manifest.json` 的文件夹；
- **按钮没有出现**：刷新 B 站首页；仍未出现时，在扩展管理页确认插件已经启用；
- **关闭 Edge 后扩展还在吗**：会保留。只要不删除或移动解压后的扩展文件夹即可；
- **Edge 提示开发人员模式扩展**：这是手动安装未上架商店扩展时的正常提示。
